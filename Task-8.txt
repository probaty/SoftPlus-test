Будет выведено 5, потому что у let область видимости это блок в котором она объявлена,
это 2 разные переменные.

При использовании 'use strict' выдало бы ошибку потому что count, на первой строке неявно объявлена  
